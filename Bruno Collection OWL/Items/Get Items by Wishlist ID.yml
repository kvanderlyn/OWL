info:
  name: Get Items by Wishlist ID
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/items/get-items
  body:
    type: json
    data: |-
      {
        "wishlist_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
