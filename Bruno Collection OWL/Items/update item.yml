info:
  name: Update Item
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/items/update-item"
  body:
    type: json
    data: |-
      {
        "id": 16,
        "name": "A shiny new item"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
