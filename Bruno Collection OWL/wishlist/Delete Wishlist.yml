info:
  name: Delete Wishlist
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/wishlists/remove-wishlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
