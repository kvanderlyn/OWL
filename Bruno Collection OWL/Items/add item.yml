info:
  name: add item
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/items/add-item
  body:
    type: json
    data: |-
      {
        "name": "table",
        "description": "sturdy and wooden, a nice place to put things"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
