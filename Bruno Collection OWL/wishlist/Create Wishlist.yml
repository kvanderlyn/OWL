info:
  name: Create Wishlist
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/wishlists/create-wishlist"
  body:
    type: json
    data: |-
      {
        "name": "Christmas List"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
