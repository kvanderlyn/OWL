info:
  name: add item
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/add-item
  body:
    type: json
    data: |-
      {
        "name": "chair",
        "description": "sturdy and wooden, a nice place to sit"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
