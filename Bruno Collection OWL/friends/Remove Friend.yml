info:
  name: Remove Friend
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/friends/remove-friend"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
