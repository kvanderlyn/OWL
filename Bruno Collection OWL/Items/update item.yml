info:
  name: update item
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/items/update-item/7
  body:
    type: json
    data: |-
      {
        "name": "wooden chair",
        "description": "sturdy and wooden, a nice place to sit"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
